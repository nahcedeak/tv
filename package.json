{
  "name": "tv",
  "private": "true",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/FinalRide/tv.git",
  "author": "decade",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.12",
    "postcss-import": "^14.0.1",
    "rimraf": "^3.0.2",
    "tailwindcss": "^2.1.2",
    "typescript": "^4.2.4",
    "vite": "^2.2.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "rimraf docs/* && vite build",
    "preview": "vite preview",
    "build:pre": "yarn build && yarn preview",
    "build:git": "yarn build && yarn git",
    "git": "git add . && git commit -m '🍎' && git push"
  }
}
